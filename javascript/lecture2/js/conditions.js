// Conditions

let number = prompt ('Enter your number');
if (number > 0 ) { 
    alert('You just inserted a positive number');
    console.log ('You just inserted a positive number ' + ' which is ' + number );
}

else{
    alert( 'You just inserted a negative number');
    console.log ('You just inserted a negative number' + ' which is ' + number);
}


// Basic Syntax
// if (condition) {
//  statement excuting condition is true
// };

// parenthess ()

// The pair of opening and closing braces defines what is called a block of code associated with an if statement. This statement represents a tests. It can result in the follwing: " if the condition is true, then excute the instruction"

// A condition is an expression that evalutes whether something is true or false. When the value of a condition true, we sat that this 

// Opetators
// >= greater than
//  <= smaller than
// !== not equal
// === Equal
// || If any of the two operands are non-zero, then the condition becomes true.


// Boolean values
//  1 is true , 0 is false, yes or no
